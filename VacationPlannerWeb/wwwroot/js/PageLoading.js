// Logic for hiding the page and showing the loading block instead while loading.
function loadingFunction() {
    $(".hidden-loading").css("display", "none");
    $(".show-loading").css("display", "block");
}
